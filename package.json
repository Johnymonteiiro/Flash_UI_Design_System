{
  "name": "flash-ui-library",
  "private": true,
  "scripts": {
    "build": "turbo build --parallel",
    "dev": "turbo dev --parallel",
    "lint": "turbo lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  
  "devDependencies": {
    "prettier": "^3.2.5",
    "turbo": "latest"
  },

  "packageManager": "pnpm@9.2.0",
  "engines": {
    "node": ">=18"
  }
}
